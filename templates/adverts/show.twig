{% extends "base.twig" %}

{% block body %}
    <h1>Объявление</h1>
    <h3>{{ advert.getTitle }}</h3>
    <p>
        Категория: {{ advert.getCategory.getTitle }}
    </p>
    <p>
        Описание: {{ advert.getDescription }}
    </p>
    <p>
        Цена: {{ advert.getPrice }} тенге
    </p>
    <a href="{{ advert.getId }}/edit"> Edit </a>
{% endblock %}