{% extends "base.twig" %}

{% block body %}
    <h1>Список объявлений</h1>

    <ul>
        {% for advert in adverts %}
            <li><a href="/adverts/{{ advert.getId() }}/edit">{{ advert.getTitle() }}</li>
        {% endfor %}
    </ul>
{% endblock %}